@model IEnumerable<WebmvcApplication22.DAL.stockuser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Company)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Company)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Price1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.Price1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Price1 })
        </td>
    </tr>
}

</table>
